# 支援週予定メーカー 操作マニュアル

特別支援学級の週予定表づくりを時短するアプリです。
交流クラスの時間割をもとに、支援児童ごとの予定と介助員の配置をまとめ、印刷・配布できます。

## 1. 起動と停止

1. フォルダ内の **start-app.cmd** をダブルクリックします
2. ブラウザが自動で開きます（開かない場合は `http://localhost:3001` にアクセス）
3. 終わるときは黒いウィンドウで **Ctrl+C** を押します

## 2. 初期設定（最初だけ）

### 2-1. 交流クラスの時間割を登録する
1. 上メニューの「交流クラス」→「＋ 新しいクラス」
2. クラス名（例：3年2組）・学年を入力します
3. 時間割の登録は2通りあります
   - **写真から読む**：「時間割の写真から読む」で紙の時間割を撮影した写真を選びます。AIが読み取った内容を確認・修正してください
   - **手入力**：表のマスを直接入力します（上段＝教科、下段＝内容）
4. 「保存する」を押します。登録後は毎週使い回せます

※ 写真取り込みには Google の APIキーが必要です（4. 参照）。

### 2-2. 支援児童を登録する
1. 「児童・介助員」で児童名を入力します
2. 交流クラスを選び、交流に行くコマをタップして指定します（交流なしも可）
3. 配慮メモ（アレルギー・声かけの工夫など）があれば入力します。児童別シートに印字されます
4. 「登録する」を押します

### 2-3. 介助員を登録する
1. 同じ画面の下部で介助員名を入力します
2. 勤務不可コマがあればタップして指定します
3. 「登録する」を押します

## 3. 毎週の流れ

1. 「週予定」で週（月曜の日付）を選び、「作成する」を押します
   - 2週目以降は「コピー元」で先週を選ぶと複写できます
2. 週を開き、「編集・印刷」を押します
3. 「今週の介助員担当」で、介助員ごとに担当児童・交流クラスにチェックを入れます（毎週変更可）
4. 「介助員を自動割付」を押すと、残りの空きコマに均等に割り振られます
5. セルを押すと内容を直接修正できます（教科・内容・担当の先生・介助員・交流／支援の切替）
6. 当日欠席が出たら、児童名の下の曜日ボタン（月〜金）で欠席にします。介助員が外れ、印刷では「欠席」と表示されます

## 4. 写真取り込みを使う場合

1. [Google AI Studio](https://aistudio.google.com/) で APIキーを取得します（無料枠あり）
2. フォルダ内の `.env.local` をメモ帳で開き、`GEMINI_API_KEY=` の後に貼り付けて保存します
3. アプリを再起動します（Ctrl+C → start-app.cmd をダブルクリック）

## 5. 印刷と保存

- 編集画面の「出力する表」で必要な表にチェックを入れます
  - 児童別：児童ごとの週予定シート（配慮メモつき）
  - 全体一覧：曜日×児童の介助員入り一覧
  - 交流クラス別：曜日×クラス×時限の1表
  - 介助員別：介助員ごとの連絡票
- 「印刷する」で紙に印刷します（表ごとに改ページされます）
- 「PDFで保存」「Excelで保存」「Wordで保存」でファイル出力もできます

## 6. データについて

- データはこのブラウザ（端末）に保存されます。別の端末では共有されません
- 週予定の Excel 出力を保管しておくと、記録として残せます

## 7. 困ったときは

| 症状 | 対処 |
|---|---|
| 写真の取り込みで401・403エラー | APIキーが間違っているか無効です。キーを確認してください |
| 写真の取り込みで429・503エラー | 一時的な混雑・上限です。自動で再試行します。続く場合は時間をおいてください |
| 保存に失敗したと出る | ブラウザの保存容量不足です。古い週予定の削除をお試しください |
| 画面が真っ白・おかしい | ブラウザの再読み込み（Ctrl+F5）をお試しください |
| 起動しない | 黒いウィンドウの文言を確認し、Node.js が必要と出たら https://nodejs.org から LTS 版をインストールしてください |
